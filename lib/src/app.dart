import 'package:flutter/material.dart';

class PushPushApp extends StatelessWidget {
  const PushPushApp({super.key});

  @override
  Widget build(BuildContext context) {
    return const MaterialApp(
      title: 'Push Push',
    );
  }
}
